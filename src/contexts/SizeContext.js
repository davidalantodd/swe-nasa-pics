// 1. create SizeContext.js file in contexts folder and import createContext
import { createContext } from 'react'

// 2. create the actual context calling it "Size Context"
export const SizeContext = createContext(null)